services:
  - type: web
    name: queue-management-system
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --workers 4 --bind 0.0.0.0:10000"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
